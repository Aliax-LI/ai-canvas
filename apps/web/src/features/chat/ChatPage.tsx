import { PlaceholderPage } from "@/components/PlaceholderPage";

export function ChatPage() {
  return (
    <PlaceholderPage
      title="GPT 对话"
      description="多模型对话界面，对应上游 gpt-chat.html。"
    />
  );
}
